Linux2, Linux2023 fluent-bit 설치 패키지
curl https://raw.githubusercontent.com/fluent/fluent-bit/master/install.sh | sh

사이트
https://docs.fluentbit.io/manual/installation/linux/amazon-linux#amazon-linux-2022


설치
curl https://raw.githubusercontent.com/fluent/fluent-bit/master/install.sh | sh
sudo systemctl start fluent-bit
sudo systemctl enable fluent-bit
sudo systemctl restart fluent-bit
sudo systemctl status fluent-bit.service

편집 경로
vi /etc/fluent-bit/fluent-bit.conf
vi /etc/fluent-bit/parsers.conf


종민이 형 방식으로 Linunx2에서 fluent-bit 설치 방법
amazon-linux-extras install epel -y

cat <<'EOF' > /etc/yum.repos.d/fluent-bit.repo
[fluent-bit]
name = Fluent Bit
baseurl = https://packages.fluentbit.io/amazonlinux/$releasever/$basearch/
gpgcheck=1
gpgkey=https://packages.fluentbit.io/fluentbit.key
repo_gpgcheck=1
enabled=1
EOF

yum update -y
yum install fluent-bit -y
find / -name fluent-bit
ln -s /opt/fluent-bit/bin/fluent-bit /usr/bin
fluent-bit --version
